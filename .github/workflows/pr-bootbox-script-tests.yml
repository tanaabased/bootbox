name: Bootbox Script Tests

on:
  pull_request:

jobs:
  bootbox-noop:
    runs-on: macos-26
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Run bootbox with repo defaults
        shell: bash
        run: ./bootbox.sh
