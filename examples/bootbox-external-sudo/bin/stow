#!/bin/bash
set -euo pipefail

if [[ "${1:-}" == "--version" ]]; then
  printf "stow 2.4.1\n"
  exit 0
fi

if [[ " $* " == *" --simulate "* ]]; then
  printf "LINK: .gitconfig => dotpkgs/git/.gitconfig\n"
  exit 0
fi

exit 0
